import '../globals.css'
export const metadata = {
  title: 'Services',
  description: '',
}

export default function ServicesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
